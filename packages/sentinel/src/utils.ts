import { ExternalApiSentinelResponse } from './models/response';

import { DEFENDER_APP_URL } from 'defender-base-client';

export function getSentinelUrl(sentinel: Pick<ExternalApiSentinelResponse, 'subscriberId'>): string {
  return ``;
}
